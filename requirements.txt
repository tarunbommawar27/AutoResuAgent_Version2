# AutoResuAgent Dependencies

# Core NLP & Embeddings
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
torch>=2.0.0

# LLM APIs
openai>=1.12.0
anthropic>=0.25.0

# Data Validation & Parsing
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0
pydantic[email]
# Async & HTTP
aiohttp>=3.9.0
asyncio

# Template Rendering
jinja2>=3.1.2

# Utilities
python-dotenv>=1.0.0
typing-extensions>=4.9.0

# Logging & Monitoring
loguru>=0.7.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development (optional)
black>=23.0.0
ruff>=0.1.0
mypy>=1.8.0

# Note: pdflatex must be installed separately on the system
# - Ubuntu/Debian: sudo apt-get install texlive-latex-base texlive-latex-extra
# - macOS: brew install --cask mactex
# - Windows: Install MiKTeX or TeX Live
